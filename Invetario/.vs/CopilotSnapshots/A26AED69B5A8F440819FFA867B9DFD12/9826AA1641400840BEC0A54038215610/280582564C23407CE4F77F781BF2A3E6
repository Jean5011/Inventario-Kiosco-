using System;
using System.Collections.Generic;
using Microsoft.Data.SqlClient;
using System.Data;
using System.Text;

namespace Invetario.Data
{
    internal class Conexion
    {
        // Reemplaza "TU_SERVIDOR" por el nombre de tu instancia de SQL (ej: . o localhost)
        private readonly string _connectionString = "Server=TU_SERVIDOR;Database=KioscoDB;Trusted_Connection=True;TrustServerCertificate=True;";

        public IDbConnection ObtenerConexion()
        {
            return new SqlConnection(_connectionString);
        }
    }
}
