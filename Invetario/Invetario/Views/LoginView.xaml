<Window x:Class="Invetario.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ingreso al Sistema" Height="350" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize" Background="#F5F6FA">
    <Grid Margin="30">
        <StackPanel VerticalAlignment="Center">
            <TextBlock Text="KIOSCO" FontSize="24" FontWeight="Bold" 
                       HorizontalAlignment="Center" Margin="0,0,0,20" Foreground="#2F3640"/>

            <TextBlock Text="Usuario:" Margin="0,10,0,5"/>
            <TextBox x:Name="txtUser" Height="35" FontSize="16" VerticalContentAlignment="Center"/>

            <TextBlock Text="Contraseña:" Margin="0,10,0,5"/>
            <PasswordBox x:Name="txtPass" Height="35" FontSize="16" VerticalContentAlignment="Center"/>

            <Button x:Name="btnLogin" Content="INGRESAR" Margin="0,30,0,0" 
                    Height="40" Background="#2980b9" Foreground="White" 
                    FontWeight="Bold" Click="btnLogin_Click" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
